"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
var core_1 = require("../core");
var CALENDAR_PERMISSIONS = [
    android.Manifest.permission.WRITE_CALENDAR,
    android.Manifest.permission.READ_CALENDAR
];
function requestCalendarPermissions(reason) {
    return core_1.requestPermission(CALENDAR_PERMISSIONS, reason)
        .then(function (args) {
        console.log('[nativescript-advanced-permissions]');
        console.log(args);
        return Object.keys(args).map(function (i) { return args[i]; }).every(Boolean);
    })
        .catch(function () { return false; });
}
exports.requestCalendarPermissions = requestCalendarPermissions;
function hasCalendarPermissions() {
    return CALENDAR_PERMISSIONS.map(function (perm) { return core_1.hasPermission(perm); }).every(Boolean);
}
exports.hasCalendarPermissions = hasCalendarPermissions;
//# sourceMappingURL=index.android.js.map